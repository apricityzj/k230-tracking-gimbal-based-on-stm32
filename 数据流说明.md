# 数据流说明：从串口坐标到舵机动作

## 一图总览

```
视觉模块 ─[串口字节流]─→ USART1_IRQHandler
                              │
                        UART_ParseByte (状态机拼帧)
                              │
                    g_offset_x/y + flag = 1
                              │
                     main() while(1) 轮询
                              │
                    apply_deadband (死区过滤)
                              │
                    PID_PD_Calc (PD控制器)
                              │
                    clamp_step (步进限幅) → 累加脉宽
                              │
                    SERVO_SetPulseUs → TIM3 PWM
                              │
                         舵机转动 (PA6/PA7)
```

## 1. 硬件接收 — USART1 中断触发

视觉模块通过串口（PA10/RX，波特率 921600）发送帧数据。每收到 1 字节，USART1 硬件置位 RXNE 标志，触发中断：

- 文件：`driver/uart.c` → `USART1_IRQHandler`
- 流程：RXNE 中断 → 读 1 字节 rx → 调用 `UART_ParseByte(rx)`

## 2. 协议解析 — 状态机逐字节拼帧

`UART_ParseByte`（`driver/uart.c`）使用 3 态状态机：

```
帧格式: [0x2C] [Xhi] [Xlo] [Yhi] [Ylo] [0x5B]
         包头    ←── 4字节数据 ──→     包尾
```

| 状态 | 行为 |
|---|---|
| `UART_RX_WAIT_HEADER` | 等到 `0x2C` → 进入 `GET_DATA` |
| `UART_RX_GET_DATA` | 连续存 4 字节到 `s_data_buf[]` → 进入 `WAIT_TAIL` |
| `UART_RX_WAIT_TAIL` | 若收到 `0x5B`，拼出 x 和 y（int16，大端序），做范围校验 ±1280 后写入全局变量 |

输出：

```c
g_offset_x = x;           // 视觉偏差X（像素）
g_offset_y = y;           // 视觉偏差Y（像素）
g_offset_update_flag = 1; // 通知主循环"有新数据"
```

## 3. 主循环轮询 — 检测更新标志

`app/main.c` 主循环不断检查 `g_offset_update_flag`：

```c
while (1) {
    if (g_offset_update_flag != 0U) {
```

一旦标志为 1，原子读取偏差值并清零标志：

```c
__disable_irq();
offset_x = g_offset_x;
offset_y = g_offset_y;
g_offset_update_flag = 0;
__enable_irq();
```

## 4. 死区滤波 — 抑制中心抖动

`apply_deadband_i16`（`app/main.c`）：

- `|offset| < 25 像素` → 强制归零，不响应
- `|offset| ≥ 25 像素` → 保留原值

目的：目标在画面中心附近时，小偏差被忽略，舵机不会来回抖。

## 5. PD 控制计算 — 生成控制量

将偏差乘以符号修正后作为反馈值输入 PD，目标值为 0（即希望偏差归零）：

```c
feedback_x = OFFSET_SIGN_X * (float)offset_x;
out_x = PID_PD_Calc(&pid_x, 0.0f, feedback_x);
```

`PID_PD_Calc`（`driver/pid.c`）内部：

```
err     = 0 - feedback = -偏差       （偏差越大，err 越大）
d_err   = err - pre_err              （变化率，抑制振荡）
output  = Kp×err + Kd×d_err          （Kp=0.85, Kd=0.26）
output  = clamp(output, ±15.0)       （限幅 ±15）
```

## 6. 脉宽增量 — 步进限幅与累加

```c
step_x = SERVO_DIR_X * out_x;         // 方向修正（±1）
step_x = clamp_step_i32(step_x, 18);  // 单帧最多 ±18us
pwm_x += step_x;                      // 累加到当前脉宽
pwm_x = SERVO_ClampPulseUs(pwm_x);    // 总脉宽限制 500~2500us
```

增量式控制：每帧在上一帧基础上叠加小步进，保证运动平滑。

## 7. 舵机输出 — PWM 写入硬件

```c
SERVO_SetPulseUs(SERVO_CH_FOR_X, pwm_x);  // CH_X → TIM3_CH1 → PA6
SERVO_SetPulseUs(SERVO_CH_FOR_Y, pwm_y);  // CH_Y → TIM3_CH2 → PA7
```

最终调用 `TIM_SetCompare1/2(TIM3, pulse)` 修改 PWM 占空比，舵机转动，云台跟踪目标。

## 关键参数汇总

| 参数 | 值 | 说明 |
|---|---|---|
| 波特率 | 921600 | 串口通信速率 |
| 包头/包尾 | 0x2C / 0x5B | 协议帧定界 |
| 偏差范围 | ±1280 | 有效像素偏差 |
| 死区 | 25 像素 | 小于此值不响应 |
| Kp / Kd | 0.85 / 0.26 | PD 控制器参数 |
| PID 输出限幅 | ±15.0 | 单次 PD 输出上限 |
| 步进限幅 | ±18 us | 单帧脉宽最大变化 |
| 脉宽范围 | 500~2500 us | 舵机安全行程 |
| 中位脉宽 | 1500 us | 舵机初始位置 |
| PWM 引脚 | PA6(X) / PA7(Y) | TIM3_CH1 / TIM3_CH2 |
